import styled from "@emotion/styled";
import { ForwardedRef, forwardRef } from "react";

interface Props {}

const FileUploader = ({}: Props, ref: ForwardedRef<HTMLDivElement>) => {
  return <></>;
};

const Component = styled.div``;

export default forwardRef(FileUploader);
